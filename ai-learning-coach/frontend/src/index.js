// Project Entry, start from here
// React 2 core packages
import React from 'react';
import ReactDOM from 'react-dom/client';

// Import the project root component
import App from './Signin';


// Render the App root component into the DOM node with the id 'root'
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render( <App />);

